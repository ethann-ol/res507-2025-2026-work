<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>QuoteBoard – Motorsport Edition</title>

  <style>
    :root {
      --bg: #0b0b0e;
      --panel: #141418;
      --line: #2a2a2f;
      --accent: #e10600; /* racing red */
      --text: #f2f2f2;
      --muted: #9a9a9a;
    }

    * {
      box-sizing: border-box;
    }

    body {
      margin: 0;
      min-height: 100vh;
      font-family: system-ui, -apple-system, BlinkMacSystemFont, sans-serif;
      background:
        repeating-linear-gradient(
          135deg,
          #0b0b0e,
          #0b0b0e 20px,
          #0f0f14 20px,
          #0f0f14 40px
        );
      color: var(--text);
      display: flex;
      justify-content: center;
      padding: 2rem 1rem;
    }


    main {
      width: 100%;
      max-width: 760px;
    }

    h1 {
      text-align: center;
      margin-bottom: 1.5rem;
      text-transform: uppercase;
      letter-spacing: 2px;
      font-weight: 800;
    }

    h1 span {
      color: var(--accent);
    }

    /* FORM */
    form {
      background: linear-gradient(180deg, #15151a, #101014);
      border: 1px solid var(--line);
      padding: 1.25rem;
      border-radius: 6px;
      display: grid;
      gap: 0.75rem;
      margin-bottom: 2rem;
      position: relative;
    }

    form::before {
      content: "";
      position: absolute;
      top: 0;
      left: 0;
      height: 4px;
      width: 100%;
      background: var(--accent);
    }

    input,
    textarea {
      width: 100%;
      font: inherit;
      padding: 0.6rem 0.75rem;
      border-radius: 4px;
      border: 1px solid var(--line);
      background: #0e0e12;
      color: var(--text);
    }

    input::placeholder,
    textarea::placeholder {
      color: var(--muted);
    }

    textarea {
      resize: vertical;
      min-height: 80px;
    }

    button {
      align-self: flex-end;
      background: var(--accent);
      color: white;
      border: none;
      border-radius: 4px;
      padding: 0.6rem 1.4rem;
      font-weight: 700;
      letter-spacing: 1px;
      text-transform: uppercase;
      cursor: pointer;
      transition: background 0.15s ease, transform 0.1s ease;
    }

    button:hover {
      background: #ff1e18;
      transform: translateY(-1px);
    }

    /* QUOTES */
    .quotes {
      display: grid;
      gap: 1rem;
    }

    .quote {
      background: linear-gradient(180deg, #15151a, #101014);
      border: 1px solid var(--line);
      border-left: 6px solid var(--accent);
      padding: 1rem 1.25rem;
      position: relative;
    }

    .quote::after {
      content: "🏁";
      position: absolute;
      top: 10px;
      right: 12px;
      opacity: 0.15;
      font-size: 1.4rem;
    }

    .quote-text {
      font-size: 1.05rem;
      line-height: 1.5;
      margin-bottom: 0.5rem;
    }

    .quote-author {
      text-align: right;
      font-size: 0.85rem;
      color: var(--muted);
      text-transform: uppercase;
      letter-spacing: 0.5px;
    }

    .empty {
      text-align: center;
      color: var(--muted);
      font-style: italic;
      border: 1px dashed var(--line);
      padding: 1.5rem;
    }

    footer {
      margin-top: 2rem;
      text-align: center;
      color: var(--muted);
      font-size: 0.8rem;
      letter-spacing: 1px;
    }
  </style>
</head>

<body>
  <main>
    <h1><span>Quote</span>Board</h1>

    <form method="post" action="/quotes">
      <input name="author" placeholder="Driver / Team (optional)" />
      <textarea name="text" placeholder="Write a racing quote..." required></textarea>
      <button type="submit">Full Throttle</button>
    </form>

    {{#if quotes.length}}
    <div class="quotes">
      {{#each quotes}}
      <div class="quote">
        <div class="quote-text">{{this.text}}</div>
        {{#if this.author}}
        <div class="quote-author">— {{this.author}}</div>
        {{/if}}
      </div>
      {{/each}}
    </div>
    {{else}}
    <p class="empty">No quotes yet. Start the engine 🏎️</p>
    {{/if}}

    <footer>
      Motorsport Edition • Flat out since lap 1
    </footer>
  </main>
</body>
</html>
